{
  "name": "@rumpel/list-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project list-sync/tsconfig.json",
    "typecheck": "tsc --noEmit --project list-sync/tsconfig.json",
    "verify": "node list-sync/dist/verify.js",
    "diff": "dotenv -e .env -- node list-sync/dist/cli.js",
    "sync": "dotenv -e .env -- node list-sync/dist/sync.js"
  },
  "dependencies": {
    "ethers": "^6.13.2",
    "yaml": "^2.4.2"
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "typescript": "^5.4.5"
  }
}
